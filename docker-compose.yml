services:
  hermes:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hermes-agent
    restart: unless-stopped
    ports:
      - "80:8080"
    env_file:
      - .env
    volumes:
      - ./data:/data
